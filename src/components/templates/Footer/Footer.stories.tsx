import { Meta } from '@storybook/react/types-6-0';
import { Footer } from '.';

export default {
  title: 'components/templates/Footer',
  component: Footer,
} as Meta;

export const Normal: React.FC = () => <Footer />;
